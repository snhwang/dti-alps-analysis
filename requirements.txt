# Dependencies for the analysis scripts in scripts/.
#
#   uv venv
#   uv pip install -r requirements.txt
#
# Versions are those the results in the manuscript were produced under, on
# Python 3.14.2. Loosen the pins if you need to. Nothing here relies on
# version-specific behaviour, but these are what the numbers came from.

pandas==3.0.3
numpy==2.4.6
scipy==1.17.1

# NIfTI reading. Needed by any script that touches image data rather than a
# cached CSV, which is about a third of them.
nibabel==5.4.2

# Figures only.
matplotlib==3.10.9

# Mixed models and clustered standard errors, used by the reliability and
# covariate analyses.
statsmodels==0.14.6

# Not on PyPI, and not needed for most of the scripts:
#
#   scripts/axis_error_sensitivity.py imports dti_alps_refined. Clone and
#   install the companion package if you want that one:
#
#       git clone https://github.com/snhwang/dti-alps-refined
#       uv pip install -e dti-alps-refined
#
#   scripts/tn_alps.py imports an internal pipeline module inside a try block.
#   The script runs without it; that path is only used when regenerating
#   tensors from source DWI rather than reading fitted ones.
#
# FSL is a separate install (https://fsl.fmrib.ox.ac.uk), not a Python package.
# Several scripts shell out to it for registration and for the tract atlases.
